name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  build-and-test:
    name: Build and test
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Restore dependencies
        run: dotnet restore ProductionManager.sln

      - name: Build
        run: dotnet build ProductionManager.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test ProductionManager.sln --configuration Release --no-build
